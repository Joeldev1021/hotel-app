import './styles.css';
const Header = () => {
    return (
        <header>
            <h1 className="title"><i className="fas fa-home"></i>otel-App</h1>
        </header>
    );
};

export default Header;
